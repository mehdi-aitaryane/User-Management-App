import { ChangePasswordValidation } from './change-password-validation';

describe('ChangePasswordValidation', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordValidation()).toBeTruthy();
  });
});
